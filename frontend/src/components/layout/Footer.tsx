export function Footer() {
  return (
    <footer className="border-t border-border/80 bg-card/70 backdrop-blur-sm">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between sm:px-6 lg:px-8">
        <p>Built for Dobby Ads Full Stack Assignment.</p>
        <p>React + Redux + shadcn + Express + MongoDB</p>
      </div>
    </footer>
  );
}
